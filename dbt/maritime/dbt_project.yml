name: maritime
version: '1.0.0'
config-version: 2
profile: maritime

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  maritime:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
